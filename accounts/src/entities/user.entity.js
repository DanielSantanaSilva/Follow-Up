export class UserEntity {
  constructor(id, name, email, password, createdDate) {
    this.id = id;
    this.name = name;
    this.email = email;
    this.password = password;
    this.createdDate = createdDate;
  }
}
